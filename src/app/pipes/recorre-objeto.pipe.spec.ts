import { RecorreObjetoPipe } from './recorre-objeto.pipe';

describe('RecorreObjetoPipe', () => {
  it('create an instance', () => {
    const pipe = new RecorreObjetoPipe();
    expect(pipe).toBeTruthy();
  });
});
